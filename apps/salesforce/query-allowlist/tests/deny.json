{
  "description": "SOQL query targeting a non-allowlisted object (User). Policy should deny with a reason.",
  "input": {
    "action": "tool_pre_invoke",
    "kind": "tool_pre_invoke",
    "mode": "input",
    "resource": {
      "type": "tool",
      "name": "salesforce-soqlquery",
      "uri": null
    },
    "payload": {
      "name": "salesforce-soqlquery",
      "args": {
        "q": "SELECT Id, Username FROM User"
      }
    }
  },
  "expected": {
    "allow": false,
    "reasonContains": "only permits querying Account, Contact, and Opportunity"
  }
}
