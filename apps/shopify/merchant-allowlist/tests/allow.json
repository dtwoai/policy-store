{
  "description": "complete_checkout where the gateway-resolved merchant (input.context.merchant) is on the buyer's allowlist (input.context.mandate.merchant_allowlist). Policy should allow.",
  "input": {
    "action": "tool_pre_invoke",
    "kind": "tool_pre_invoke",
    "mode": "input",
    "resource": {
      "type": "tool",
      "name": "ucp-shopify-complete_checkout",
      "uri": null
    },
    "payload": {
      "name": "ucp-shopify-complete_checkout",
      "args": {
        "id": "chk_123"
      }
    },
    "context": {
      "merchant": "approved-airline",
      "mandate": {
        "merchant_allowlist": ["approved-airline", "approved-hotel"]
      }
    },
    "subject": {
      "claims": {
        "sub": "agent-7"
      }
    }
  },
  "expected": {
    "allow": true
  }
}
