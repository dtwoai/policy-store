{
  "description": "Out-of-scope egress response. The tool name suffix-matches search_catalog, not get_order / get_checkout, so the policy applies no transform and the response is returned unchanged. This policy is transform-only and never denies; 'deny' here means 'no redaction transform' (pass-through), which is the negative case for a transform policy.",
  "input": {
    "action": "tool_post_invoke",
    "kind": "tool_post_invoke",
    "mode": "output",
    "subject": {
      "claims": {
        "sub": "agent-7f3c",
        "groups": ["commerce-agents"]
      }
    },
    "resource": {
      "type": "tool",
      "name": "shopify-mcp-server-search_catalog",
      "uri": null
    },
    "payload": {
      "name": "shopify-mcp-server-search_catalog",
      "args": {
        "query": "earbuds"
      },
      "result": {
        "products": [
          { "id": "prod_22", "title": "Wireless Earbuds", "price": 7999 }
        ]
      }
    }
  },
  "expected": {
    "allow": true,
    "transformApplied": false
  },
  "reasonContains": "search_catalog is not get_order / get_checkout, so no redaction transform is applied (pass-through)"
}
