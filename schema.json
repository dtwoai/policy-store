{
  "schemaVersion": "1.0.0",
  "description": "Describes the expected fields for the policy-store manifest.json index, the individual policy markdown files, and the optional per-policy tests file. Policy entries, md files, or tests which do not follow this schema will not be accepted into the repository.",
  "manifest": {
    "description": "The top-level manifest.json file: the searchable index of every published policy and the folder maps for apps, industries and bundles.",
    "fields": {
      "policies": {
        "name": "policies",
        "description": "Array of policy entries. Each entry holds the search metadata for one published policy and the path to its folder; see policyEntry for the shape of each entry. The full policy body lives in the policy's policy markdown file.",
        "type": "PolicyEntry[]",
        "required": true
      },
      "apps": {
        "name": "apps",
        "description": "Map of app slug to the relative path of the app's folder. Each folder includes, at least, a README.md file.",
        "type": "Record<string, string>",
        "required": true
      },
      "industries": {
        "name": "industries",
        "description": "Map of industry slug to the relative path of the industry's folder. Each folder includes, at least, a README.md file.",
        "type": "Record<string, string>",
        "required": true
      },
      "bundles": {
        "name": "bundles",
        "description": "Map of bundle slug to the relative path of the bundle's folder. Each folder includes, at least, a README.md file.",
        "type": "Record<string, string>",
        "required": true
      }
    },
    "policyEntry": {
      "description": "The shape of each object in manifest.policies. Carries the search metadata to find a policy, the policyChecksum that pins its body, and the path to its folder; the description and policy body live in the policy markdown file rather than in the manifest.json file.",
      "fields": {
        "path": {
          "name": "path",
          "description": "The relative path from manifest.json to the policy folder.",
          "type": "string",
          "required": true
        },
        "name": {
          "name": "name",
          "description": "User-visible name of the policy.",
          "type": "string",
          "required": true
        },
        "tags": {
          "name": "tags",
          "description": "A list of tags associated with the policy.",
          "type": "string[]",
          "required": true
        },
        "publishedAt": {
          "name": "publishedAt",
          "description": "The date the policy was published, as an ISO 8601 date (e.g. 2026-06-02).",
          "type": "string (ISO 8601 date)",
          "required": true
        },
        "direction": {
          "name": "direction",
          "description": "Intended use for the policy. One of: egress, ingress.",
          "type": "string (egress | ingress)",
          "required": true
        },
        "apps": {
          "name": "apps",
          "description": "A list of applications this policy is designed for.",
          "type": "string[]",
          "required": true
        },
        "industries": {
          "name": "industries",
          "description": "An optional list of industries this policy may be useful for.",
          "type": "string[]",
          "required": false
        },
        "bundles": {
          "name": "bundles",
          "description": "An optional list of use-case bundles this policy is a part of.",
          "type": "string[]",
          "required": false
        },
        "experimental": {
          "name": "experimental",
          "description": "Whether the policy is experimental: a reviewed starting point that has not been manually validated against a test setup of the app (it compiles and passes its unit tests, but confirm it against your instance of the app before relying on it). Always present in the manifest; defaults to false when the policy markdown omits it.",
          "type": "boolean",
          "required": true
        },
        "policyChecksum": {
          "name": "policyChecksum",
          "description": "Checksum of the policy body, in the form sha256:<hex>. Used to verify the integrity of the policy markdown file; it must equal the SHA-256 of the (trimmed) Rego body.",
          "type": "string (sha256:<hex>)",
          "required": true
        },
        "schemaVersion": {
          "name": "schemaVersion",
          "description": "The schema version this policy was published with.",
          "type": "string",
          "required": true
        },
        "minimumGatewayVersion": {
          "name": "minimumGatewayVersion",
          "description": "The minimum DTwo Gateway version required to use this policy.",
          "type": "string",
          "required": false
        }
      }
    }
  },
  "policy": {
    "description": "An individual policy markdown file: YAML frontmatter holding the policy metadata, followed by the document body which contains the policy itself inside a fenced ```rego block.",
    "fields": {
      "name": {
        "name": "name",
        "description": "User-visible name of the policy.",
        "type": "string",
        "required": true
      },
      "tags": {
        "name": "tags",
        "description": "A list of tags associated with the policy.",
        "type": "string[]",
        "required": true
      },
      "publishedAt": {
        "name": "publishedAt",
        "description": "The date the policy was published, as an ISO 8601 date (e.g. 2026-06-02).",
        "type": "string (ISO 8601 date)",
        "required": true
      },
      "description": {
        "name": "description",
        "description": "A description of what the policy does. This should include the intent of the policy.",
        "type": "string",
        "required": true
      },
      "direction": {
        "name": "direction",
        "description": "Intended use for the policy. One of: egress, ingress.",
        "type": "string (egress | ingress)",
        "required": true
      },
      "apps": {
        "name": "apps",
        "description": "A list of applications this policy is designed for.",
        "type": "string[]",
        "required": true
      },
      "industries": {
        "name": "industries",
        "description": "An optional list of industries this policy may be useful for.",
        "type": "string[]",
        "required": false
      },
      "bundles": {
        "name": "bundles",
        "description": "An optional list of use-case bundles this policy is a part of.",
        "type": "string[]",
        "required": false
      },
      "experimental": {
        "name": "experimental",
        "description": "Optional. When true, marks the policy as experimental: a reviewed starting point that has not been manually validated against a test setup of the app. It compiles and passes its unit tests; confirm it behaves as intended against your instance of the app before relying on it. Omit or set false for validated policies. Defaults to false.",
        "type": "boolean",
        "required": false
      },
      "schemaVersion": {
        "name": "schemaVersion",
        "description": "The schema version this policy was published with.",
        "type": "string",
        "required": true
      },
      "minimumGatewayVersion": {
        "name": "minimumGatewayVersion",
        "description": "The minimum DTwo Gateway version required to use this policy.",
        "type": "string",
        "required": false
      },
      "policy": {
        "name": "policy",
        "description": "The plain-text Rego policy. This is not a frontmatter field; it is the document body inside the fenced ```rego block.",
        "type": "string (Rego)",
        "required": true
      }
    }
  },
  "policyTest": {
    "description": "Each object in a policy folder's optional tests array (the file is an array of these). Imported alongside the policy as a runnable policy test case.",
    "fields": {
      "description": {
        "name": "description",
        "description": "A description of what this test verifies.",
        "type": "string",
        "required": true
      },
      "input": {
        "name": "input",
        "description": "The input object for the test. The shape of the input should match what the policy expects, but ultimately it's up to the user to define valid tests.",
        "type": "object (JSON)",
        "required": true
      },
      "output": {
        "name": "output",
        "description": "The expected outcome of evaluating the policy against the input.",
        "type": "TestOutput",
        "required": true
      }
    },
    "testOutput": {
      "description": "The expected outcome of evaluating a policy against a test input.",
      "fields": {
        "expectedResult": {
          "name": "expectedResult",
          "description": "The decision the policy is expected to return for this input. One of: allow, deny.",
          "type": "string (allow | deny)",
          "required": true
        },
        "expectedReason": {
          "name": "expectedReason",
          "description": "An optional reason which might be returned with the policy decision.",
          "type": "string",
          "required": false
        }
      }
    }
  }
}
