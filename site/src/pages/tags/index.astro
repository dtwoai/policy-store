---
import Base from '../../layouts/Base.astro';
import { getTags } from '../../lib/catalog';

const tags = getTags();
const title = 'Policy tags';
const description =
  'Find policies by what they do — access-control, dlp, pii, secrets, redaction, governance — and by ingress or egress. Useful when the shape of the control matters more than the app.';

const jsonLd = {
  '@context': 'https://schema.org',
  '@type': 'CollectionPage',
  name: title,
  description,
  url: new URL('/tags', Astro.site!).href,
};
---

<Base
  title={title}
  description={description}
  jsonLd={jsonLd}
  breadcrumbs={[{ name: 'Home', path: '/' }, { name: 'Tags' }]}
>
  <h1>{title}</h1>
  <p class="lede">{description}</p>
  <p>
    {
      tags.map(({ tag, count }) => (
        <a class="tag" href={`/tags/${tag}`}>{tag} ({count})</a>
      ))
    }
  </p>
</Base>
